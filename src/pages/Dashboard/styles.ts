import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  paddingLocation: {
    paddingTop: '18px',
  },
  autoCompleteSelect: {
    marginTop: '10px',
    '& fieldSet': {
      borderColor: '#424E63',
    },
  },
})
